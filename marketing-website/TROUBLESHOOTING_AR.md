# 🛠️ حل المشاكل - نشر موقع Pizoo

## 🔍 دليل استكشاف الأخطاء وحلها

---

## مشكلة 1: لا أستطيع تحميل ملف ZIP

### الأعراض:
- الرابط لا يعمل
- التحميل يفشل
- الملف صغير جداً (أقل من 300 KB)

### الحلول:
```bash
# جرّب من terminal/bash:
curl -O https://pizoo-landing.preview.emergentagent.com/api/download/marketing-website

# أو:
wget https://pizoo-landing.preview.emergentagent.com/api/download/marketing-website -O pizoo-website.zip
```

### بديل:
أخبرني وسأنشئ لك رابط بديل أو طريقة أخرى!

---

## مشكلة 2: Vercel لا يقبل الملف

### الأعراض:
- رسالة خطأ عند الرفع
- "Invalid file format"
- "Upload failed"

### الحلول:

**الحل 1:** تأكد من الملف
```bash
# تحقق من حجم الملف:
ls -lh pizoo-website-build.zip
# يجب أن يكون: 385K تقريباً

# تحقق من نوع الملف:
file pizoo-website-build.zip
# يجب أن يرى: Zip archive data
```

**الحل 2:** فك الضغط أولاً
1. فك ضغط الملف
2. افتح مجلد `build`
3. ارفع محتويات `build` مباشرة إلى Vercel

**الحل 3:** استخدم GitHub
بدلاً من رفع ZIP، ارفع المجلد إلى GitHub ثم اربطه بـ Vercel.

---

## مشكلة 3: Build يفشل في Vercel

### الأعراض:
```
Error: Build failed
Exit code: 1
```

### الحلول:

**إذا رفعت ملف build جاهز:**
- تأكد من اختيار "Output Directory" = `build`
- اترك "Build Command" فارغاً

**إذا رفعت الكود المصدري:**
```
Build Command: yarn build
Output Directory: build
Install Command: yarn install
```

---

## مشكلة 4: الموقع يظهر صفحة فارغة

### الأعراض:
- الصفحة بيضاء
- console يظهر أخطاء
- "Cannot GET /"

### الحلول:

**الحل 1:** تحقق من vercel.json
تأكد من وجود:
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

**الحل 2:** تحقق من الـ Logs
1. في Vercel Dashboard
2. اذهب إلى Deployments
3. افتح آخر deployment
4. اذهب إلى "Function Logs"
5. ابحث عن الأخطاء

---

## مشكلة 5: النطاق لا يربط

### الأعراض:
```
❌ Invalid Configuration
⚠️ Pending
```

### الحلول:

**الحل 1:** انتظر
- DNS يستغرق وقتاً (15 دقيقة - 48 ساعة)
- تحقق من الحالة كل ساعة

**الحل 2:** تحقق من DNS
اذهب إلى: https://dnschecker.org
اكتب: `pizoo.ch`
تحقق من أن DNS يشير إلى Vercel

**الحل 3:** تحديث Nameservers
تأكد من تغيير Nameservers في مزود النطاق إلى:
```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

**الحل 4:** حذف وإعادة إضافة
1. في Vercel، احذف النطاق
2. انتظر 5 دقائق
3. أضف النطاق مرة أخرى

---

## مشكلة 6: SSL لا يعمل

### الأعراض:
```
⚠️ Not Secure
🔓 Connection is not secure
```

### الحلول:

**الحل 1:** انتظر
SSL يحتاج وقتاً بعد ربط النطاق (5-30 دقيقة)

**الحل 2:** تحقق من حالة النطاق
في Vercel Domains:
```
✅ Valid Configuration ← يجب أن يكون هكذا
```

**الحل 3:** أعد تنشيط SSL
1. في Vercel → Domains
2. انقر على النطاق
3. ابحث عن "Renew Certificate"
4. انقر عليه

---

## مشكلة 7: الموقع بطيء

### الأعراض:
- تحميل بطيء
- PageSpeed score منخفض

### الحلول:

**الحل 1:** تفعيل Edge Network
في Vercel Settings:
- تأكد من تفعيل Edge Functions
- تفعيل Automatic Compression

**الحل 2:** تحسين الصور
الصور في الموقع الحالي محسّنة، لكن يمكنك:
- استخدام Vercel Image Optimization
- تصغير أحجام الصور أكثر

**الحل 3:** تفعيل Caching
تحقق من headers في vercel.json:
```json
"headers": [
  {
    "source": "/static/(.*)",
    "headers": [
      {
        "key": "Cache-Control",
        "value": "public, max-age=31536000, immutable"
      }
    ]
  }
]
```

---

## مشكلة 8: تبديل اللغة لا يعمل

### الأعراض:
- انقر على اللغة ولا شيء يتغير
- اللغة تعود للإنجليزية

### الحلول:

**الحل 1:** تحقق من Browser Console
1. افتح Developer Tools (F12)
2. اذهب إلى Console
3. ابحث عن أخطاء JavaScript

**الحل 2:** مسح Cache
```bash
# في المتصفح:
Ctrl+Shift+R (Windows/Linux)
Cmd+Shift+R (Mac)
```

**الحل 3:** تحقق من i18n
تأكد من أن ملفات اللغة موجودة:
```
src/i18n/locales/en.json
src/i18n/locales/de.json
src/i18n/locales/ar.json
# إلخ...
```

---

## مشكلة 9: الموقع يعمل على vercel.app لكن ليس على pizoo.ch

### الأعراض:
- `pizoo-marketing.vercel.app` يعمل ✅
- `pizoo.ch` لا يعمل ❌

### الحلول:

**الحل 1:** تحقق من DNS
```bash
# من terminal:
nslookup pizoo.ch
# أو:
dig pizoo.ch
```

**الحل 2:** تحقق من Nameservers
```bash
# من terminal:
nslookup -type=ns pizoo.ch
```
يجب أن ترى:
```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

**الحل 3:** مسح DNS Cache
```bash
# على Mac:
sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder

# على Windows (CMD as Admin):
ipconfig /flushdns

# على Linux:
sudo systemd-resolve --flush-caches
```

---

## مشكلة 10: أخطاء في Console

### الأعراض:
```
Failed to load resource
404 Not Found
CORS error
```

### الحلول:

**404 Not Found:**
- تحقق من المسارات في الكود
- تأكد من وجود rewrites في vercel.json

**CORS Error:**
- أضف CORS headers في vercel.json
- تحقق من API endpoints

**Failed to load resource:**
- تحقق من مسارات الملفات
- تأكد من رفع جميع الملفات

---

## 🆘 ما زلت عالقاً؟

### معلومات مطلوبة للمساعدة:

عند طلب المساعدة، أخبرني بـ:

1. **في أي خطوة أنت؟**
   - مثال: "الخطوة 5 - إعدادات المشروع"

2. **ماذا حدث بالضبط؟**
   - مثال: "ظهرت رسالة خطأ عند الرفع"

3. **أي رسائل خطأ؟**
   - انسخ النص الكامل للخطأ

4. **لقطة شاشة (إذا أمكن)**
   - صورة للخطأ أو الصفحة

### سأساعدك فوراً! 😊

---

## 📞 طرق التواصل

أخبرني هنا مباشرة وسأرد عليك!

مثال:
```
"واجهت مشكلة في الخطوة 8 - النطاق يظهر 'Invalid Configuration'"
```

---

## ✅ نصائح للوقاية

لتجنب المشاكل:

1. **اتبع الخطوات بالترتيب**
2. **لا تتخطى أي خطوة**
3. **اقرأ رسائل الخطأ بعناية**
4. **جرّب في متصفح Incognito/Private**
5. **احتفظ بنسخة احتياطية**

---

**آخر تحديث:** ديسمبر 2025
